#!/usr/bin/env bash

# Reference:
# https://evanhahn.com/scripts-i-wrote-that-i-use-all-the-time/
# https://codeberg.org/EvanHahn/dotfiles/src/branch/main/home/bin/bin/cpwd
#
# `cpwd` copies the current directory to the clipboard. Basically `pwd | copy`.

set -e
set -u
set -o pipefail

pwd | tr -d '\n' | copy
